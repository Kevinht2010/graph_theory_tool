
class Components {
    constructor(G) {
        this.components = this.componentCount(G);
    }

    componentCount = (G) => {
        
    }

    getComponentCount = () => { return this.components; }
}

export default Components
